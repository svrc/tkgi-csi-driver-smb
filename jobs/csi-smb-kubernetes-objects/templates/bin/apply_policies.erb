#!/usr/bin/env bash

set -e
[ -z "$DEBUG" ] || set -x

config_dir="/var/vcap/jobs/csi-smb-kubernetes-objects/config/"
kubectl="/var/vcap/packages/kubernetes/bin/kubectl --kubeconfig=${config_dir}/kubeconfig"

until ${kubectl} apply -f "${config_dir}/policies/"; do
 sleep 2
done

